/**
 * Sanity CLI configuration
 * Used for sanity CLI commands (deploy, etc.)
 */

import { defineCliConfig } from "sanity/cli";

export default defineCliConfig({
  api: {
    projectId: "gn3kobnn",
    dataset: "o9S0oqRLk",
  },
});
